/** Support contact shown on Forgot Password / Rejected screens. */
export const SUPPORT_EMAIL = 'support@zay.ma';

/** Country dial code — MA only for MVP (see critique issue #7). */
export const PHONE_DIAL_CODE = '+212';

/** Local national-number length after the dial code. */
export const PHONE_NATIONAL_LENGTH = 9;

/** Minimum acceptable password length, mirrored from backend Joi schema. */
export const PASSWORD_MIN_LENGTH = 8;
