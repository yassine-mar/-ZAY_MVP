# Public env vars (prefix with EXPO_PUBLIC_ — embedded in the JS bundle at build time).
# Do NOT put secrets here. The mobile app cannot keep secrets.

EXPO_PUBLIC_API_URL=http://localhost:5000/api/v1
EXPO_PUBLIC_SENTRY_DSN=
EXPO_PUBLIC_POSTHOG_KEY=
