// Default Metro config from Expo.
// Customizations (SVG transformer, monorepo paths) belong here if needed.
const { getDefaultConfig } = require('expo/metro-config');
module.exports = getDefaultConfig(__dirname);
